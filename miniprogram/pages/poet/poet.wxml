<!-- miniprogram/pages/poet/poet.wxml -->
<view class="poet">
  <view class="name">{{poetData.poet}}</view>
  <view class="content" wx:for='{{poetData.abstract}}' wx:key='{{index}}' >
    {{item}}
  </view>
</view>